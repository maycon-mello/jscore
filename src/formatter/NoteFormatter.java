/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package com.mzm.score.formatter;

import com.mzm.score.bar.Beat;
import com.mzm.score.bar.note.Note;
import java.util.ArrayList;
import java.util.List;

/**
 *
 * @author maycon.mellos
 */
public class NoteFormatter {

    public static void format(Beat beat) {
        //erase beams
        //List<Note> beamNotes = new ArrayList<>();
        //for(Note n:notes){
            //n.setBeam(null);
        //}
        //
        //List<Beat> beats = NoteFormatter.getBeats(notes);
        //
        //for(Beat beat:beats){
            /*for(int i = 0; i < notes.size(); i++){
                Note currentNote = notes.get(i);
                Note nextNote;
                Note lastNote;
                if(notes.size() > i + 1){
                    nextNote = notes.get(i+1);
                }
                if(i > 0){
                    lastNote = notes.get(i - 1);
                }
            }*/
        //}
    }
    //
    public static void createBeam(List<Note> n){
        
    }
    //
}

